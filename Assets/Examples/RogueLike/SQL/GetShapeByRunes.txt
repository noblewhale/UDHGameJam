SELECT B.name FROM rune_shapes AS A 
JOIN shapes AS B ON A.shape_id = B.id
WHERE rune_id = @rune1_id AND craft_order = 0 
AND shape_id IN (SELECT shape_id FROM rune_shapes WHERE rune_id = @rune2_id AND craft_order = 1);